let drugs=[];const $=id=>document.getElementById(id);async function init(){drugs=await fetch('drugs.json').then(r=>r.json());$('drug').innerHTML=drugs.map((d,i)=>`<option value="${i}">${d.name}</option>`).join('');$('count').textContent=`現在 ${drugs.length} 製剤を収載。`;$('data').textContent=JSON.stringify(drugs,null,2)}function fmt(n,d=2){return Number(n.toFixed(d)).toString()}function parsePrescribed(text){if(!text)return null;const m=text.trim().replace(/,/g,'').match(/^([0-9]*\.?[0-9]+)\s*(mg|g|mL|ml)$/i);if(!m)return null;const v=Number(m[1]),u=m[2].toLowerCase();return{value:v,unit:u==='ml'?'mL':u}}function toMg(v,u,d){if(u==='mg')return v;if(u==='g')return v*1000;if(u==='mL'&&d.mg_per_ml)return v*d.mg_per_ml;return null}function toFormulation(vMg,d){if(d.mg_per_g)return{value:vMg/d.mg_per_g,unit:'g'};if(d.mg_per_ml)return{value:vMg/d.mg_per_ml,unit:'mL'};return null}function status(v,min,max){if(v<min-1e-4)return['warning','添付文書通常量より少ない'];if(v>max+1e-4)return['warning','添付文書通常量より多い'];return['ok','添付文書通常量の範囲内']}function compareDaily(p,min,max,d){const pmg=toMg(p.value,p.unit,d);if(pmg===null)return`<div class="warning">⚠ 処方量を成分量へ換算できません。製剤濃度を確認してください。</div>`;const [c,m]=status(pmg,min,max),form=toFormulation(pmg,d);let doseText=form?`（製剤量 ${fmt(form.value,2)} ${form.unit}/日）`:'';return`<div class="${c}"><strong>1日量監査：${m}</strong><br>処方成分量 ${fmt(pmg,1)} mg/日 ${doseText}<br>基準成分量 ${fmt(min,1)}～${fmt(max,1)} mg/日${d.mg_per_g?`（${fmt(min/d.mg_per_g,2)}～${fmt(max/d.mg_per_g,2)} g/日）`:''}</div>`}function calc(){const d=drugs[+$('drug').value],w=+$('weight').value,age=+$('age').value,prescribedInput=$('prescribed').value,p=parsePrescribed(prescribedInput);if(!w||w<=0)return show('体重を入力してください。','warning');if(prescribedInput&&!p)return show('1日量は「1.5 g」「300 mg」「6 mL」のように、数字＋単位をキーボードで入力してください。','warning');let h=`<h2>${d.name}</h2><div class="muted">${d.active} / ${d.form} / ${d.concentration}</div>`;if(d.rule_type==='age'){if(!age&&age!==0)return show('この薬剤は年齢区分で用量が決まるため、年齢を入力してください。','warning');const r=d.age_rules.find(x=>age>=x.min&&age<x.max);if(!r)return show('入力年齢は添付文書の対象範囲外です。','warning');const freqText=d.frequency||'添付文書記載回数';h+=`<div class="result-main">${fmt(r.g,2)} g/回</div><div>成分量：${r.mg} mg/回　｜　${freqText}</div><div class="ok">年齢 ${age} 歳 → 固定用量を適用。体重比例換算はしていません。</div>`;if(p){const freqMatch=freqText.match(/(\d+)回/);const freq=freqMatch?Number(freqMatch[1]):null;const minDay=r.mg*(freq||1),maxDay=r.mg*(freq||1);h+=compareDaily(p,minDay,maxDay,d);if(!freq)h+=`<div class="warning">ℹ 添付文書の1日回数を自動確定できないため、1日量基準は要確認です。</div>`}}else{const minDose=Math.min(w*d.dose_mg_per_kg_min,d.max_mg_per_dose||Infinity),maxDose=Math.min(w*d.dose_mg_per_kg_max,d.max_mg_per_dose||Infinity),mind=Math.min(w*d.dose_mg_per_kg_min,d.max_mg_per_day||Infinity),maxd=Math.min(w*d.dose_mg_per_kg_max,d.max_mg_per_day||Infinity);const mgPerG=d.mg_per_g||100;h+=`<div class="result-main">1日量目安：${fmt(mind,1)}～${fmt(maxd,1)} mg/日</div><div>製剤量換算：${fmt(mind/mgPerG,2)}～${fmt(maxd/mgPerG,2)} g/日</div><div class="muted">体重 ${w} kg × ${d.dose_mg_per_kg_min}～${d.dose_mg_per_kg_max} mg/kgを基準に1日量を算出。${d.max_mg_per_day?`1日最大${d.max_mg_per_day}mgを反映。`:''}</div><div class="muted">参考：1回成分量 ${fmt(minDose,1)}～${fmt(maxDose,1)} mg</div>`;if(p)h+=compareDaily(p,mind,maxd,d)}h+=`<div class="warning">⚠ ${d.note}</div><p class="muted">出典：${d.source}</p>`;$('result').innerHTML=h;$('result').classList.remove('hidden')}function show(m,c){$('result').innerHTML=`<div class="${c}">${m}</div>`;$('result').classList.remove('hidden')}$('calc').addEventListener('click',calc);$('showData').addEventListener('click',()=>$('data').classList.toggle('hidden'));init();if('serviceWorker'in navigator)navigator.serviceWorker.register('service-worker.js');